<UserControl x:Class="Gallery.Controls.controlLogin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gallery.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="Login:" Margin="1,1,5,1"/>
        <TextBox x:Name="textboxLogin" Grid.Column="1" Margin="1"/>
        <TextBlock Text="Password:" Grid.Row="1" Margin="1,1,5,1"/>
        <TextBox x:Name="textboxPassword" Grid.Column="1" Grid.Row="1" Margin="1"/>
        <Button Content="Log in" Grid.Row="2" Grid.ColumnSpan="2" VerticalAlignment="Bottom" Click="LoginClick"/>

    </Grid>
</UserControl>
